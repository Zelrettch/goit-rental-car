import { createAction } from "@reduxjs/toolkit";

export const setParams = createAction("catalog/setParams");
export const clearItems = createAction("catalog/clearItems");

export const setPage = createAction("catalog/setPage");
