export const selectParams = (state) => state.catalog.searchParams;

export const selectHasNext = (state) => state.catalog.hasNext;

export const selectPage = (state) => state.catalog.page;

export const selectItems = (state) => state.catalog.items;